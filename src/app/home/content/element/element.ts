export type ElementMetric = {
  label: string;
  value: string;
  detail: string;
  tone: string;
};

export type ElementChartPoint = {
  label: string;
  orders: number;
  users: number;
};

export type ElementTableRow = {
  name: string;
  role: string;
  status: string;
  completion: string;
};
