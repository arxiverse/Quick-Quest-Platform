export type DashboardCarouselItem = {
  title: string;
  subtitle: string;
  accent: string;
};

export type DashboardQuestItem = {
  title: string;
  owner: string;
  role: string;
  category: string;
  points: string;
  reward: string;
  score: string;
};

